//DO NOT EDIT THIS LINE:
const _replaceMe_ = null;

// Assign each variable so that it is the type that its name indicates
// Replace _replaceMe_ with your answer

// Example:
const numberVar = 7;

const stringVar = 'string';
const booleanVar = true;
const nullVar = null;
const undefinedVar = undefined;

// Use math operators to assign the following variables.
// Use Math methods where ever possible.
const pi = 3.1415926;

// Example:
const piPlusOne = pi+1;

const piMinusOne = pi - 1;
const piDividedByThree = pi / 3;
const piSquared = Math.pow(pi, 2);
const piRoundedDown = Math.floor(pi);
const piRoundedUp = Math.ceil(pi);

// Assign the variables below using x and y.
// Use methods wherever possible.
const x = 7.5;
const y = -15;

//Example:
const isXLessThanY = x < y;

const xMinusY = x - y;
const yDividedByX = y / x;
const xAsAString = x.toString();
const YToThePowerOfX = Math.pow(y, x);
const yRoundedUp = Math.ceil(y);

// Stretch Task:
// Research the following string methods: includes, indexOf, slice, toUpperCase, toLowerCase
// Using those methods on 'sampconstext', correctly assign the following variables.
const sampconstext = "Isn't documentation great!";

//Example:
const trueValue = sampconstext.includes('great!');

const falseValue = sampconstext.includes('z');
const three = sampconstext.indexOf("'");
const nine = sampconstext.indexOf("u");
const cumen = sampconstext.slice(8, 12);
const eat = sampconstext.slice(22, 24);
const DOC = sampconstext.slice(6, 8).toString();
const is = sampconstext.slice(0, 1).toLowerCase;
